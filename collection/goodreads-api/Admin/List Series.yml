info:
  name: List Series
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/admin/series"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
