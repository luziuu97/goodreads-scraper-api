info:
  name: Search
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/book/search?query=wild huntress"
  params:
    - name: query
      value: wild huntress
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
