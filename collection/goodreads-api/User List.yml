info:
  name: User List
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/user/:userId/list/:listName?page=1&per_page=10&sort=review&order=d"
  params:
    - name: page
      value: "1"
      type: query
    - name: per_page
      value: "10"
      type: query
    - name: sort
      value: review
      type: query
    - name: order
      value: d
      type: query
    - name: listName
      value: read
      type: path
    - name: userId
      value: "179234404"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
