info:
  name: Book
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/book/details/:bookId?reviews=false"
  params:
    - name: reviews
      value: "false"
      type: query
    - name: bookId
      value: "207567925"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
