info:
  name: Book
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/book/details/:bookId?provider=hardcover"
  params:
    - name: provider
      value: hardcover
      type: query
    - name: editionId
      value: "32963227"
      type: query
      disabled: true
    - name: bookId
      value: "1662524"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
