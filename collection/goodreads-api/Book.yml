info:
  name: Book
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/book/details/:bookId"
  params:
    - name: provider
      value: hardcover
      type: query
      disabled: true
    - name: editionId
      value: "32963227"
      type: query
      disabled: true
    - name: bookId
      value: "1913404"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
