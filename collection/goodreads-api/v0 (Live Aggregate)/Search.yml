info:
  name: Search
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/book/search?query=1984&type=all&language=de"
  params:
    - name: query
      value: "1984"
      type: query
    - name: type
      value: all
      type: query
    - name: language
      value: de
      type: query
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: provider
      value: aggregate
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Save `isbn` + `isbn10` + `languageCode` from the hit.
  Do not persist Hardcover `id` (e.g. 714600) as the details key.
  `language` is a strict filter. `format` on edition is lowercase.
