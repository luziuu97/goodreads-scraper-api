info:
  name: List Genres
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/admin/genres"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
