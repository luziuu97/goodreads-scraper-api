info:
  name: List Authors
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/admin/authors"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
