info:
  name: List Editions
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/admin/editions"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
