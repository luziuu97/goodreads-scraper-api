info:
  name: User Challenge
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/user/challenge/:userId"
  params:
    - name: reviews
      value: "false"
      type: query
      disabled: true
    - name: userId
      value: "162488673"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
