info:
  name: User
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/:userId/details"
  params:
    - name: reviews
      value: "false"
      type: query
      disabled: true
    - name: userId
      value: "179234404"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
